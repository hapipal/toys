'use strict';

// Load modules

const Lab = require('lab');
const Code = require('code');
const Toys = require('..');

// Test shortcuts

const lab = exports.lab = Lab.script();
const describe = lab.describe;
const it = lab.it;
const expect = Code.expect;

describe('Toys', () => {

    it('', (done) => done(new Error()));
});
